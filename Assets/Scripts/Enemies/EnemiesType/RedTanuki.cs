using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedTanuki : EnemySpawn
{

    // Start is called before the first frame update
    void Start()
    {
        maxHealth = 220;
        health = 220;
        currency = 6;
        score = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
