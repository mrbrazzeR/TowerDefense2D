using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tanuki : EnemySpawn
{

    // Start is called before the first frame update
    void Start()
    {
        maxHealth = 130;
        health = 130;
        currency = 4;
        score = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
   
}
